name: C++ Build
'on':
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y libzip-dev libeigen3-dev nlohmann-json-dev libspdlog-dev
          
      - name: Install Google Test v1.12.1
        run: |
          git clone -b release-1.12.1 https://github.com/google/googletest.git
          cd googletest
          mkdir build
          cd build
          cmake ..
          sudo make
          sudo make install
      - name: Install mio
        run: |
          git clone -b master https://github.com/vimpunk/mio.git
          cd mio
          mkdir build
          cd build
          cmake ..
          sudo make
          sudo make install
      - name: Build C++ project
        run: |
          cmake -B build
          make
